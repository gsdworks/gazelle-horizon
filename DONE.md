# DONE — Gazelle Books

_Append-only permanent log of everything completed and discovered. Newest at the bottom. Never edit existing lines. FULL detail welcome — findings, email facts, decisions, reasoning; this file is the complete record and can grow without limit._
_Full detail for anything here lives in gazelle-context-working.md (the monolith) or git history._

2026-07-07 — 18 metafield definitions bulk-created via Admin API (`create_gazelle_metafields.py`, created=18 failed=0), verified against a live title.
2026-07-08 — Product page built: bottom band (tabs/specs/subjects rail), cover treatment, hero layout.
2026-07-14 — Footer built and styled to mockup (native four-column + utilities bar). Header built native-first (~zero custom CSS). Global page-width fix: `gazelle-layout.liquid` = single source of 1200px width.
2026-07-16 — Featured Collection carousel bleed fixed (contained to 1200px, 6 cards + peek). Full product export audited: force-update had reached only ~3% of catalogue — Dave's fixes correct, propagation was the problem. Fred workstream reconciled: A–N "regression" was a wrong-file artefact; real spec is A–Y and complete.
2026-07-17 — AS400 outbound built and delivered end-to-end via EZ Exporter (25 cols, dual-address proven). Stock import blocked on header row identified and requested. GZ order-ID prefix set.
2026-07-20 — Both import pipelines proven end-to-end in Shopify (stock + tracking), verified against store exports. Split shipment proven. Consolidated email sent to Fred.
2026-07-27 — Fred's four loops closed (header space stripped, duplicate barcode removed at source, order-ref echo confirmed, partial-quantity moot — no backorders). Billy re-engaged, go-live reframed off a date.
2026-07-30 — Diagnosed and fixed the failed force-update root cause: 3 metafield definitions (author_bio, reviews, prize_achievements) were multi-line, blocking Dave's single-line writes. Recreated all three as single line via admin (unstructured-metafields path), values preserved (229/104/7 verified). Confirmed remaining 20 definitions already match Dave's JSON. Emailed Dave to re-push, flagged the series-token bug. Built `update_gazelle_metafield_types.py` (dry-run-safe, backup-first) — committed unused as the tool for the future date/list type migration.
2026-07-30 — Context system restructured: split into CLAUDE.md (procedure) + STATUS.md (now) + DONE.md (log) + build-context.sh, with `/update-context` slash command. Monolith parked as read-only reference pending a later carve into docs/.
2026-07-30 — Metafield root cause CONFIRMED AND FIXED. Dave's metafields.json (sent 29 Jul, cc Billy/Paul) shows his push writes all 23 fields as `single_line_text_field`. Only 3 definitions mismatched (multi-line): `author_bio` (229 values), `reviews` (104), `prize_achievements` (7). Fixed via admin using the unstructured-metafields path — delete "field only" → View unstructured metafields → Add definition on the key (the blank Add-definition form false-errors "namespace and key already taken"; the unstructured path is the working route). All values preserved, counts verified 229/104/7, storefront access kept off to match originals. All 23 definitions now match Dave's payload exactly. Corroborating evidence: 46 new products created overnight 29–30 Jul landed with the 18 newer metafields populated and zero placeholder junk — the creation path was already flowing where the 3 sparse fields had no data, which also means "failing since 7 July" was only true of the update path. Verified before fixing: no stored value in any of the 3 fields contains a newline; paragraphs are encoded as HTML (`<p>`/`<br>`) so single-line loses nothing.
2026-07-30 — Emailed Dave (reply-all on his metafields thread, Billy cc): definitions fixed, re-push requested, will verify against an export straight after. Flagged NEW BUG: `custom.series` arrives as internal tokens (`__line:the_frean_chronicles`) on all 385 products that have one, not the series name. Deliberately did NOT raise the `%sh_%` placeholder-token issue — unproven whether it is his template or import-side; check after the re-push (if the 895 `%sh_height_mm%` values clear on their own it was never his; if they persist, raise with evidence).
2026-07-30 — Metafield review of all 23 fields against the fresh export: single line validates everywhere (zero newlines in any field). Confirmed minimal future type-migration list: `publication_date` (to Date; currently 100% free-text like "23 May 2023", 0 ISO — blocks the New-Release rule) plus `author`, `thema_subjects`, `bic_subjects`, `bisac_subjects`, `main_subjects` (to list types — blocks native S&D subject filtering). Nothing else ever needs to move. TRAP: pipes occur inside legitimate content (book title "Short | Poto") — any future list-splitting must be per-field, never blanket. Also found: `custom.format` and `custom.genre` are undefined orphan keys on 2 products each — not in the 23-field contract, unexplained, investigate when convenient.
2026-07-30 — Catalogue population finding: the store holds ~1,800 of ~54,500 titles (~3%), growing ~40–50/day via the event-driven creation path. The full bulk load has NEVER run and is the true go-live gate (force-update across the full catalogue reportedly crashes BooksoniX). Next escalation once the re-push verifies: ask Dave (Billy cc) for the bulk-load plan and timeline.
2026-07-30 — `update_gazelle_metafield_types.py` built (dry-run default, `--execute` gated on an existing backup, `deleteAllAssociatedMetafields` hardcoded false with a runtime guard) but NEVER executed — the token exchange failed on placeholder credentials before reaching Shopify. Kept in the repo as the tool for the future type migration. NOTE: the BooksoniX Integration app client secret may have been rotated by Dave — unverified; the 400 `application_cannot_be_found` came from placeholder creds, not a rotation test.
2026-07-30 — git push from CLI fixed via SSH: `id_ed25519` generated, GitHub host key pinned to `known_hosts`, `origin` switched to `git@github.com:gsdworks/gazelle-horizon.git`. Authenticates as `gsdworks`; both pending commits pushed. `upstream` (Shopify/horizon) deliberately left on HTTPS — fetch-only from a public repo, never needs auth.
2026-08-01 — `@docs/build-manual.md` import VERIFIED WORKING in a fresh Claude Code session. Test: asked the session what the build manual says about "ruling out the native path" with no file read and no prompting toward a file. It returned the full section — the ❌ "I couldn't find a setting for this in the files" vs ✅ "`section_width` controls X but not Y; `force-full-width` is applied unconditionally at `resource-list.liquid:98` with no setting in its control path" contrast, the name-the-condition rider, the say-so-and-ask rider, and the 16 July carousel incident that the rule was written from (diagnostic traced the cause perfectly then jumped to a CSS override without asking whether a section setting governed it; Grant ruled it out in the theme editor). It also correctly connected the neighbouring rules (propose-before-build with `[NATIVE SETTING]`/`[NATIVE BLOCK]`/`[SCOPED CSS]` labels, audit the Horizon library first, stop-and-ask when the native path isn't obvious). This closes the open concern that neither the import nor `/update-context` was ever exercised in the session that created them — the CLAUDE.md → build-manual import chain loads on its own in a cold session, so the build manual can hold detail without it going unread.
2026-08-01 — Eye Books set up to GSD Project Standard v1.1 in a separate repo (`gsdworks/eyebooks-horizon`). Two findings from that setup carry back to Gazelle:
  (a) **GitHub permits only ONE fork of a given upstream repo per account.** `gsdworks/gazelle-horizon` holds that slot for `Shopify/horizon`, so Eye Books could not be forked at all. **All future projects therefore use a plain repo with a mirror-push of the base theme plus an `upstream` remote**, which behaves identically for everything we actually do (fetch upstream, sync, diff). **Gazelle stays as a fork** — it works, and converting it would be churn for zero gain. It is the only fork there will ever be; nobody should later "fix" the newer repos to match Gazelle's shape, because the fork path is the one that is unavailable, not the plain-repo path.
  (b) **Shopify CLI self-upgraded 4.5.2 → 4.6.0 unprompted, mid-command, during a `shopify theme pull`.** It is a global npm install, so **Gazelle is now on 4.6.0 too with no action taken in this repo.** Diagnostic rule that follows: if a CLI command behaves differently one day for no apparent reason, **check the version first** — the tool can move under you between sessions without anyone upgrading it.
  Correction owed: `gazelle-tech-stack.md` (the derived doc on the claude.ai side, not in this repo) records **Shopify CLI 3.92.1**, two major versions stale. The same stale `3.92.1` also sits in the monolith at `gazelle-context-working.md:759`. Fix both at the monolith carve.
