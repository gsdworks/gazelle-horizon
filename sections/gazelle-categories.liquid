{% comment %}
  Section: gazelle-categories.liquid
  Display name: "Icon Row"

  A row of circular icon links. Reusable — add it more than once per page with
  different content (e.g. one instance for browse categories, one for publisher
  highlights). Each instance holds its own blocks and settings.

  ⚠️ FILENAME: stays `gazelle-categories.liquid` deliberately. The section `type`
  in templates/index.json is `gazelle-categories`; renaming the file would orphan
  the live section. Only the schema display name changed (16 July).

  16 July changes:
  - Renamed "Category Icons" → "Icon Row" (no longer category-specific).
  - Icons can be IMAGES (PNG/SVG) as well as emoji. Image wins when set; emoji is
    the fallback. Per-block, so instances can mix.
  - Image fills the circle (object-fit: cover) and is clipped by the circle's
    border-radius + overflow:hidden, so artwork with square or white corners still
    reads as a circle.
  - Grey placeholder plate suppressed on image blocks (the artwork IS the circle);
    still shown behind emoji blocks.
  - CSS scoped to the section instance (#gazelle-icon-row-{{ section.id }}), so two
    instances on one page can be styled independently without a selector collision.
  - Heading default blank; preset lean (3 blocks).
  - ICON SIZE is now a theme-editor setting (60–280px). Everything derives from it
    via --gz-icon-size:
      * item width tracks the circle (min 88px so short labels don't cramp)
      * gap scales with size, clamped 16–48px, so big circles don't look cramped
      * emoji scales at ~35% of the circle
      * MOBILE AUTO-CAPS at min(size, 120px) — a 280px circle on a 375px phone
        would show ~1.3 items. Deliberate: no separate mobile control needed.
    NOTE: there is deliberately NO "columns per row" setting. Size + wrap gives the
    per-row count implicitly (e.g. 260px → 4 across in a 1200px row) and cannot
    overflow. A columns setting would fight the size slider.
  - max_blocks raised 12 → 16.
  - Links were already supported (block.settings.link) — unchanged.
{% endcomment %}

{%- liquid
  assign icon_size = section.settings.icon_size | default: 90
-%}

<section
  class="gazelle-icon-row"
  id="gazelle-icon-row-{{ section.id }}"
  {{ section.shopify_attributes }}
>
  <div class="gazelle-icon-row__inner">

    {% if section.settings.heading != blank %}
      <h2 class="gazelle-icon-row__heading">{{ section.settings.heading }}</h2>
    {% endif %}

    <div class="gazelle-icon-row__grid">
      {% for block in section.blocks %}
        {% if block.type == 'category' %}
          <a
            class="gazelle-icon-row__item"
            href="{{ block.settings.link | default: '#' }}"
            {{ block.shopify_attributes }}
          >
            <div class="gazelle-icon-row__circle{% if block.settings.image != blank %} has-image{% endif %}">
              {% if block.settings.image != blank %}
                <img
                  class="gazelle-icon-row__image"
                  src="{{ block.settings.image | image_url: width: 560 }}"
                  alt=""
                  width="{{ block.settings.image.width }}"
                  height="{{ block.settings.image.height }}"
                  loading="lazy"
                >
              {% else %}
                <span class="gazelle-icon-row__emoji" aria-hidden="true">{{ block.settings.emoji }}</span>
              {% endif %}
            </div>
            {% if block.settings.label != blank %}
              <span class="gazelle-icon-row__label">{{ block.settings.label }}</span>
            {% endif %}
          </a>
        {% endif %}
      {% endfor %}
    </div>

  </div>
</section>

{% style %}
  #gazelle-icon-row-{{ section.id }} {
    /* Single source of truth for this instance's sizing. */
    --gz-icon-size: {{ icon_size }}px;
    padding: 52px 24px;
    background: #ffffff;
  }

  #gazelle-icon-row-{{ section.id }} .gazelle-icon-row__inner {
    max-width: 1200px;
    margin: 0 auto;
  }

  #gazelle-icon-row-{{ section.id }} .gazelle-icon-row__heading {
    text-align: center;
    font-size: 1.75rem;
    font-style: italic;
    font-weight: 400;
    color: #1a1a1a;
    margin: 0 0 40px;
  }

  #gazelle-icon-row-{{ section.id }} .gazelle-icon-row__grid {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    /* Gap scales with the circle so big icons don't crowd. */
    gap: clamp(16px, calc(var(--gz-icon-size) * 0.3), 48px)
         clamp(16px, calc(var(--gz-icon-size) * 0.25), 40px);
  }

  #gazelle-icon-row-{{ section.id }} .gazelle-icon-row__item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 12px;
    text-decoration: none;
    color: inherit;
    /* Track the circle, but never so narrow that a short label cramps. */
    width: max(var(--gz-icon-size), 88px);
  }

  #gazelle-icon-row-{{ section.id }} .gazelle-icon-row__circle {
    width: var(--gz-icon-size);
    height: var(--gz-icon-size);
    border-radius: 50%;
    background: #eef0f3;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: background 0.2s ease, transform 0.2s ease;
    /* Clips image artwork to the circle regardless of the source file's corners. */
    overflow: hidden;
    flex: 0 0 auto;
  }

  /* The artwork is the circle — no grey plate behind it. */
  #gazelle-icon-row-{{ section.id }} .gazelle-icon-row__circle.has-image {
    background: transparent;
  }

  #gazelle-icon-row-{{ section.id }} .gazelle-icon-row__item:hover .gazelle-icon-row__circle {
    background: #dde0e6;
    transform: scale(1.07);
  }

  /* Keep image blocks from flashing a grey plate on hover. */
  #gazelle-icon-row-{{ section.id }} .gazelle-icon-row__item:hover .gazelle-icon-row__circle.has-image {
    background: transparent;
  }

  #gazelle-icon-row-{{ section.id }} .gazelle-icon-row__image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    border-radius: 50%;
  }

  #gazelle-icon-row-{{ section.id }} .gazelle-icon-row__emoji {
    /* Scale the emoji with the circle. */
    font-size: calc(var(--gz-icon-size) * 0.35);
    line-height: 1;
    display: block;
  }

  #gazelle-icon-row-{{ section.id }} .gazelle-icon-row__label {
    font-size: 0.875rem;
    text-align: center;
    font-weight: 500;
    color: #1a1a1a;
    line-height: 1.3;
  }

  @media (prefers-reduced-motion: reduce) {
    #gazelle-icon-row-{{ section.id }} .gazelle-icon-row__circle {
      transition: none;
    }
    #gazelle-icon-row-{{ section.id }} .gazelle-icon-row__item:hover .gazelle-icon-row__circle {
      transform: none;
    }
  }

  @media (max-width: 640px) {
    #gazelle-icon-row-{{ section.id }} {
      /* Cap on mobile — a 280px circle on a 375px phone shows ~1.3 items. */
      --gz-icon-size: min({{ icon_size }}px, 120px);
      padding: 36px 0;
    }

    #gazelle-icon-row-{{ section.id }} .gazelle-icon-row__heading {
      padding: 0 16px;
      text-align: left;
      margin-bottom: 24px;
    }

    #gazelle-icon-row-{{ section.id }} .gazelle-icon-row__grid {
      display: flex;
      flex-wrap: nowrap;
      justify-content: flex-start;
      overflow-x: auto;
      -webkit-overflow-scrolling: touch;
      scrollbar-width: none;
      gap: 16px;
      padding: 0 16px 12px;
    }

    #gazelle-icon-row-{{ section.id }} .gazelle-icon-row__grid::-webkit-scrollbar {
      display: none;
    }

    #gazelle-icon-row-{{ section.id }} .gazelle-icon-row__item {
      flex: 0 0 auto;
      width: max(var(--gz-icon-size), 80px);
    }

    #gazelle-icon-row-{{ section.id }} .gazelle-icon-row__label {
      font-size: 0.8rem;
    }
  }
{% endstyle %}

{% schema %}
{
  "name": "Icon Row",
  "tag": "section",
  "class": "gazelle-icon-row-section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "info": "Optional. Leave empty for no heading."
    },
    {
      "type": "range",
      "id": "icon_size",
      "label": "Icon size",
      "min": 60,
      "max": 280,
      "step": 10,
      "unit": "px",
      "default": 90,
      "info": "Circle diameter on desktop. Bigger icons fit fewer per row and the rest wrap — e.g. 260px gives 4 across. Capped at 120px on mobile so the row stays scrollable."
    }
  ],
  "blocks": [
    {
      "type": "category",
      "name": "Icon",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Icon image (optional)",
          "info": "Square image. Use at least 2x your icon size (e.g. 560px for a 280px circle) so it stays sharp on retina. If empty, the emoji below is used."
        },
        {
          "type": "text",
          "id": "emoji",
          "label": "Icon (emoji)",
          "default": "📚",
          "info": "Only used when no icon image is set."
        },
        {
          "type": "text",
          "id": "label",
          "label": "Label",
          "info": "Optional. Leave empty when the artwork already carries the name."
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link",
          "info": "Can link to a collection, page, or any URL"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Icon Row",
      "blocks": [
        { "type": "category" },
        { "type": "category" },
        { "type": "category" }
      ]
    }
  ],
  "max_blocks": 16
}
{% endschema %}
